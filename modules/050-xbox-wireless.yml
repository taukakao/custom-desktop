name: xbox-wireless
type: shell
commands:
- apt-get install -y git dkms cabextract make
- git clone https://github.com/dlundqvist/xone
- cd xone
- sed -i "s/dkms install /dkms install -k $(cat /usr/share/vanilla/kernel-version) /" install.sh
- make install
- cd ..
- rm -r xone
